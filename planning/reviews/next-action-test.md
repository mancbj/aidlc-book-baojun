# Next Action 30-Second Test

> D09-T03「完成 30 秒下一动作测试」审查记录。  
> 生成时间：2026-07-23T02:44:00Z  
> 范围：`site/index.html`、`site/progress.html`

## 结论

通过。

首次访问者打开驾驶舱或生产线页后，无需滚动即可看到下一项 Must：

`D09-T03 · 完成 30 秒下一动作测试`

该下一动作同时可通过键盘 Tab 快速聚焦，并直达 `details.html#task-D09-T03`。

## 测试前发现的问题

初次检查时：

- `site/index.html` 首屏可见 `D09-T03`，但完整任务标题位于 900px 视口下方。
- `site/progress.html` 需要先进入“任务下钻”区域才能看到 D09-T03，对首次访问者不够直接。

因此本轮将“下一动作”提升到驾驶舱与生产线页的 Hero 右侧元信息区，并做成原生链接。

## 自动检查方法

使用 Codex bundled Node.js + bundled Playwright 包，并指定本机 Chrome 可执行文件运行真实浏览器检查。

检查项：

1. 设置视口为 `1280 × 900`。
2. 打开 `site/index.html` 和 `site/progress.html`。
3. 检查首屏是否可见 `D09-T03`。
4. 检查首屏是否可见完整标题 `完成 30 秒下一动作测试`。
5. 连续按 Tab，确认第 9 次 Tab 内可聚焦到 `details.html#task-D09-T03`。

## 证据摘要

| 页面 | 首屏可见任务 ID | 首屏可见完整标题 | 键盘可聚焦 | 焦点位置 | 结果 |
|---|---|---|---|---:|---|
| `site/index.html` | 是 | 是 | 是 | 第 9 次 Tab | 通过 |
| `site/progress.html` | 是 | 是 | 是 | 第 9 次 Tab | 通过 |

## 关闭标准映射

验收项：`首次使用者 30 秒内找到下一项 Must`

- 下一项 Must 在首屏可见：通过。
- 下一项 Must 具备稳定任务 ID 与完整标题：通过。
- 下一项 Must 可点击进入任务事实：通过。
- 下一项 Must 可由键盘快速聚焦：通过。

## 后续建议

D10-T01 进入 GitHub 协作模板阶段时，可沿用这个模式：每个外部协作者入口都应有“下一步是什么、点哪里、产物是什么”的首屏提示。
