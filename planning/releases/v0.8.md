# v0.8 ALREADY & KEEP-EXT Release

> 本文件记录 v0.8 发布准备。机器门禁报告将在 RC 打包后写入 `releases/v0.8-rc/readiness.json`。  
> 正式 GitHub Release 回执在公开发布后写入 `releases/v0.8/release.json`。

## 发布目标

- 版本：`v0.8`
- 主题：ALREADY 与 KEEP-EXT 合同化；不改写 triage 为 SHIP
- 目标批次：`EXP-07-01`、`EXP-08-01`、`EXP-04-03`、`EXP-05-03`
- Loop 目标分：≥ 92 / 100（见 `planning/publication/v0.8-scorecard.json`）
- 已知缺口：`READER-RESPONSES`（known-gap）

## 策略

- Policy：[`planning/releases/v0.8-policy.json`](v0.8-policy.json)
- `html_required=true`
- `pdf_required=true`
- `next_version=v0.9-draft`

## 正式发布回执

- Status: `preparing`
- Machine receipt: 待 `releases/v0.8/release.json`
