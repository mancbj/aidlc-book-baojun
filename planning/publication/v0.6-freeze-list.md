# v0.6 Freeze List

## 术语与章节边界

- 必须保留：`𝓔 = Engineering with Exsecutio`
- CH-07 Verify 不等于 CH-08 Runtime Verify
- specs.md 是参考实现，不是 AI-DLC 唯一形态

## 实验边界

- `planned` / `ready` 不得写成 `verified`
- Bolt 类型选择是规则建议，不替代人工判断
- 回滚桌面演练只证明时间线与 Runbook 缺口可复现，不证明生产恢复能力
- 检查点预算不证明穷尽全部风险
- 价值记分卡不证明业务价值已被因果证实
- 需要真实模型方差的实验本轮不伪装为 verified

## Reader 边界

- 只有真实回复才能把 Reader 槽位改为 `responded`
- 若 v0.6 发布前仍少于三位回复，继续记录 `READER-RESPONSES` known-gap

## 发布边界

- readiness `ready` 可以携带 Reader known-gap
- `.artifacts/v0.6-loop/` 不入库
