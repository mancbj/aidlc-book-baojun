# v0.8.001 Freeze List

## 术语

- `𝓔 = Engineering with Exsecutio`
- CH-07 Verify ≠ CH-08 Runtime Verify

## 版本

- 本 Loop 只发布 `v0.8.001`
- 下一 patch 为 `v0.8.002`，不默认跳到 `v0.9`

## 实验边界

- triage 保持 `KEEP-EXT`
- 只验证冻结 pin / 本地复现夹具
- 不得把 `planned` / `ready` 伪装成 `verified`
- specs.md 是参考实现，不是唯一标准

## Reader

- 无真实回复则 deferred + `READER-RESPONSES` known-gap
- 禁止伪造 `responded`

## 发布

- `.artifacts/v0.8.001-loop/` 不入库
- zip 只上 GitHub Release，不进仓库
