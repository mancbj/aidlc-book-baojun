# v0.3 Freeze List

在 Loop 结束前，下列事实边界不得被“润色”改写为更强结论。

## 术语

- 必须保留：`𝓔 = Engineering with Exsecutio`
- 禁止自动改成：`Execution`

## 证据状态

- `EXP-08-01`：ready（可引用为入口/设计，不写成已验证生产结论）
- `EXP-08-02` / `EXP-08-03`：planned
- 其他实验：以 `progress/experiments.json` 为准；`planned` 不得写成 `verified`

## 章节边界

- CH-07 Verify ≠ CH-08 Runtime Verify
- Operations 工具链不得写成成熟生产能力；保留 alpha / planned 边界
- 不把 specs.md 参考实现写成 AI-DLC 唯一形态

## 已知缺口

- `READER-RESPONSES`：known-gap（v0.3 可保留为诚实缺口，不得伪造读者反馈）

## 发布卫生

- release HTML/PDF 不得出现：Metadata 表、Writing Sprint Card、Draft Completeness、Status Source、章节 Gate checklist、Review Notes、Dxx 写作验收清单、核心问题去重审计（写作过程件）
- 源稿 `book/**` 可继续保留上述写作脚手架；剥离只发生在 release profile
