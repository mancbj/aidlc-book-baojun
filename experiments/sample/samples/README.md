# Sample Inputs

本目录只保存可公开、无密钥的 JSON 输入夹具。

- `input.json`：合法候选分解；D05-T01 用它生成 `../output/sample.json`。
- `invalid/`：D05-T02 已添加五类失败样例；每个文件只突出一个主要错误。

输入可以由人或 AI 提议，但提交前必须去除隐私、密钥和不可分发数据。实验只校验结构追踪性，不判断业务语义正确性。
