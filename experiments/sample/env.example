# EXP-03-01 requires no environment variables, API tokens, or secrets.
# Python 3.9+ standard library is the complete runtime dependency.
# Keep this file comments-only unless a future task changes the experiment contract.
